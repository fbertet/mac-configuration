---
- name: Ensure ghostty config directory exists
  ansible.builtin.file:
    path: "{{ ansible_env.HOME }}/.config/ghostty"
    state: directory
    mode: "0755"

- name: Deploy ghostty config file
  ansible.builtin.copy:
    src: config
    dest: "{{ ansible_env.HOME }}/.config/ghostty/config"
    mode: "0644"
